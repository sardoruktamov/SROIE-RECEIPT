spring:
  application:
    name: security-project
  datasource:
    url: jdbc:postgresql://localhost:5432/yandexgo_db
    username: yandexgo
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
